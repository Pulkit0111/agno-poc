# console/.env.test.local.example
# Before running `npx playwright test`, export the SAME CONSOLE_SESSION_SECRET the running
# backend uses (see the repo root .env), e.g.:
#   export CONSOLE_SESSION_SECRET=<value from repo root .env>
# Then in one terminal: uv run bott-app (repo root)
# In another: cd console && npx playwright test
