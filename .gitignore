# Secrets — never commit
.env
.secrets/
*.pem

# Runtime state
*.db
*.db-shm
*.db-wal
*.log

# Agentic workspace (sandboxed scratch for file/terminal/code tools)
.bott_workspace/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# agent-ui (Phase 4 scaffold) — separate Next.js repo, excluded for now
agent-ui/

# Local tooling / harness state
.claude/
.superpowers/
# Local design/plan docs (not tracked)
docs/
agentos.db
agentos.db-*
.env.workspace-b
